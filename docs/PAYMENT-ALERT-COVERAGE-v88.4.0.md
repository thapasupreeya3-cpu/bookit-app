# Payment and alert coverage — v88.4.0

Reviewed 13 September 2026 against the current release source and the original 41-case alert inventory. This closes out the inventory with an explicit disposition for every case; it does not claim all 41 are fully automated.

**Coverage:** 15 implemented, 12 retained, 13 partial, 1 missing.

“Implemented” means a source workflow added or completed in this release. “Retained” means an existing source control is preserved. “Partial” names the remaining gap. These statuses do not certify a deployed browser, delivery to a real inbox, production payment accounts or bank settlement.

`NO` is the invoice number, `B` a booking ID, `PID` a participant ID and `TOKEN` an invoice-specific bearer token. Invoice review requires a signed-in authorised participant/helper; a public payment token permits payment after review, not approval of support. Helper invoice links can include `&for=PID`.

The compact Next actions badge covers current actionable groups. Calendar and List share the review/payment notice. Payment tracking distinguishes a recorded receipt from an unverified deposit into the business bank. The payer can use a configured card/PayTo checkout or the supplied receiving details; automatic saved-payment mandates and wages are separate unfinished integrations.

| Case | Alert/event | Current coverage | Current destination | Behaviour and remaining limit | Source |
|---|---|---|---|---|---|
| A01 | Participant timesheet approval | Implemented | `#/bookings` → `#/invoice?invoice=NO`; legacy review → `#/journey?panel=shift&booking=B` | Calendar and List share a persistent review/payment notice. Invoice reviews replace duplicate timesheet tasks. Calendar cells themselves remain visit summaries. | UI, tasks |
| A02 | Global participant action count | Implemented | `#/journey` | Next actions and mobile badges count current actionable groups for workers, participants, scoped helpers and office staff. Optional feedback and personal waiting-on-office tasks are excluded. | UI, routes |
| A03 | Review deadline approaching | Implemented | `#/journey`; `#/invoice?invoice=NO` | Pending invoice reviews expose the existing seven-day calendar deadline where approval_from is present; fallback timesheet tasks also carry a deadline. Due and overdue grouping remains visible. | payments, tasks |
| A04 | First review request to selected reviewer | Implemented | `#/invoice?invoice=NO&for=PID`; fallback `#/bookings?for=PID` | Initial submission/invoice review notices select the nominated currently authorised reviewer. Invoice deep links verify helper access before selecting the participant. | server, UI |
| A05 | Obsolete queued approval email | Implemented | Exact invoice or visit in the review email | Initial review notices carry requires_approval metadata. The outbox suppresses obsolete pending-review messages; existing access-stamp checks suppress changed or revoked recipient access. | server, tasks |
| A06 | Unanswered review reminders | Partial | `#/journey`; `#/journey?panel=shift&booking=B` | The existing three-day reminder and seven-day deemed timesheet policy remain. Deemed approval does not authorise online collection. A dedicated ageing-review office escalation and delivery-confirmed review policy are not added. | server, payments |
| A07 | Approval email preference or delivery delay | Implemented | `#/journey`; office `#/journey?panel=deliveries` | Required tasks remain visible through the consolidated badge and booking notice even when optional timesheet email preferences suppress messages. Office delivery failures remain actionable. Actual inbox delivery is unverified. | UI, tasks |
| A08 | Worker has not finished the shift record | Partial | `#/journey?panel=shift&booking=B` | Worker Next actions names an ended visit needing its note. A dedicated completion reminder cadence, due date and ageing office escalation are not added. | tasks |
| A09 | Participant questions a timesheet | Partial | Worker `#/journey?panel=shift&booking=B`; invoice `#/invoice?invoice=NO` | Questions pause collection and create the worker review task and notification. The legacy query path emails the office; an ageing dispute task and common office escalation for every invoice-query path remain absent. | server, tasks, payments |
| A10 | Worker answers a timesheet question | Implemented | `#/journey`; `#/invoice?invoice=NO` | A worker answer restarts the participant review task/notice. Issued invoice collection remains paused until explicit approval; a pending fallback visit task still links to the invoice review controls. | server, tasks, UI |
| A11 | Approved shift not invoiced | Partial | `#/admin/money`; `#/journey?panel=finance` | Durable billing jobs retry and show specific waiting reasons. They now issue eligible new self/private shifts on submission. Each waiting billing job still lacks its own named office task. | billing, UI |
| A12 | Invoice issued | Implemented | `/pay/TOKEN`; `#/invoice?invoice=NO` | Invoice snapshot, PDF email and delivery status are retained. New self/private submissions invoice immediately after cutover; plan-managed and NDIA routes still wait for approval. Transport must be configured. | server, billing, payments |
| A13 | Invoice due or overdue | Implemented | `#/invoice?invoice=NO`; `#/journey` | Open self/private invoice tasks show balance and due date; overdue notices go to the payer on the first overdue day and then weekly. Settled, withdrawn, paused and processing payments are excluded. | payments, UI |
| A14 | Plan manager owes payment | Partial | Participant `#/statements` / invoice; payer `/pay/TOKEN`; office `#/payment-tracking` | Statements and invoice screens say sent to the plan manager without asking the participant to pay. Plan-manager payer links and reminders work in source. A dedicated aged plan-manager invoice office task is not added. | payments, UI |
| A15 | Payment link cannot be created | Implemented | Invoice payment page; office `#/payment-tracking` | Checkout creation persists retry state. The payer sees preparation status; repeated failures create an office task. Temporary preparation cannot change recorded shift approval or mark payment received. | payments, UI |
| A16 | Expired payment link | Partial | `/pay/TOKEN`; `#/invoice?invoice=NO` | Confirmed expiry/failure permits a replacement checkout for the remaining balance. A bearer link can expire; the signed-in invoice remains available. Universal recovery from every late/missing expiry notification is not certified. | payments, UI |
| A17 | Failed or delayed electronic payment | Implemented | Invoice payment page; office `#/payment-tracking` | Signed failed/delayed payment events are persisted. Processing and retry states are distinct, failed payment prompts a retry, and in-flight collection options are suppressed. Provider credentials and webhooks require validation. | payments, UI |
| A18 | Saved-payment authorisation | Missing | No automatic-payment mandate setup screen | Reusable saved-method mandates and unattended off-session collection are not implemented. Card/PayTo checkout is initiated by the payer; a stored invoice or prior approval is not ongoing payment consent. | payments |
| A19 | Successful payment receipt | Implemented | Receipt email → `/pay/TOKEN`; invoice payment history | A confirmed allocated receipt queues one receipt notice per ledger entry and updates the balance. Repeated events reuse ledger identifiers. This is a queued receipt email, not proof of inbox delivery. | payments, finance |
| A20 | Refund or provider dispute | Partial | `#/journey?panel=finance`; `#/payment-tracking` | Refund/dispute provider events create named office tasks and clickable dashboard actions. Provider deadlines are not imported; refunds, reversals and dispute responses still require office/provider work. | payments, finance |
| A21 | Unmatched or excessive bank receipt | Implemented | `#/payment-tracking` → Match payment | Configured Zai notifications fetch and verify receipt details, match safe references and retain unmatched/excess amounts. Needs-matching and part-matched receipts expose scoped, idempotent partial allocation. No ordinary-business-bank feed is connected. | payments, Zai, UI |
| A22 | NDIA claim needs submission or is rejected | Partial | `#/admin/money` → NDIA claim file | Claim CSV preparation and local tracking remain. External NDIA submission, acceptance, rejection ingestion and a corresponding automated rejected-claim workflow are not connected. | server, UI |
| A23 | Payroll draft needs work | Partial | `#/journey?panel=payroll` | Draft, exception, review and independent approval controls remain, with office tasks. Gross pay, super, allowances and calculation evidence still need review; no payroll provider calculates or pays wages here. | finance, payroll |
| A24 | Older work missed by payroll schedule | Implemented | `#/journey?panel=payroll` | Scheduled drafts now recover eligible unbatched work from the configured cutover to yesterday in bounded 90-day periods. Source-key deduplication prevents repeat inclusion; no automatic wage transfer is introduced. | finance, recovery tests |
| A25 | Worker payment status and payslip | Partial | `#/journey?panel=payroll` | Worker notices and status distinguish preparation, export-file creation and payment recorded by office. Bank-confirmed wages, payment-failure ingestion, automatic payslip retrieval and payslip notifications remain disconnected. | payroll, UI |
| A26 | Invoice withdrawal outcome | Retained | `#/admin/money`; invoice page | v88.3.4 withdrawal history and payer notice are retained. Collection links are retired/revoked and payment reminders suppressed; reissue preserves history and uses a new invoice number. Real provider behaviour remains unverified. | lifecycle, payments |
| A27 | Worker booking request | Retained | `#/bookings`; `#/journey?panel=shift&booking=B` | The worker booking-request badge and dated response action remain. Consolidated Next actions also includes the request; resolving it clears the outstanding task. | calendar, tasks |
| A28 | Booking accepted, cancelled or changed | Partial | `#/bookings`; `#/journey?panel=shift&booking=B` | Booking changes remain visible in their records and existing emails. No new persistent acknowledgement feed for every accepted, changed or cancelled booking is introduced. | server, calendar |
| A29 | Urgent cover or near-term unanswered booking | Retained | `#/journey?panel=shift&booking=B`; office task queue | Cover offers, near-term responses and office coverage tasks retain their time-sensitive escalation and expiry checks. | tasks, server |
| A30 | Unread messages | Retained | `#/messages` | Unread-message indicators and existing notification handling remain separate from the action count. | UI, server |
| A31 | Missing, rejected or expiring worker files | Retained | `#/account/credentials`; office `#/admin/verification` | Missing, rejected and expiring document actions retain their checklist links, verification states and office ownership. Related uploads remain grouped. | tasks, verification |
| A32 | Training due or expiring | Retained | `#/account/training` | Outstanding or expiring training retains its grouped Next action, individual module details and established reminders. | tasks, UI |
| A33 | Verification assignment and follow-ups | Retained | `#/admin/verification`; `#/account/credentials` | Reviewer assignment, due queues, combined document requests and bounded verification follow-ups remain. | verification |
| A34 | Support plan updated or awaiting review | Retained | `#/support-plan`; relevant visit / office review | Existing support-plan review tasks, readiness checks and changed-plan records remain. This release does not add a universal change-diff notification screen. | tasks, server |
| A35 | Account access invitation or revocation | Retained | `#/access`; invitation link; `#/clients` | Scoped invitations, revocation and access checks remain. New invoice reads, helper selection and action counts recheck the selected participant and required scopes. | server, routes, payments |
| A36 | Recruitment applications and decisions | Partial | `#/journey?panel=recruitment`; `#/my-jobs`; `#/my-applications` | Recruitment checks and existing job/application notifications remain. Every job application, offer or decision is not projected into the consolidated action list. | tasks, server |
| A37 | Incident or complaint requires follow-up | Retained | `#/admin/compliance`; office Next actions | Incident/complaint follow-up clocks and operational escalation remain. Deployment must still verify the configured deadlines and actual reporting evidence. | tasks, server |
| A38 | First-visit feedback | Retained | `#/journey?panel=followup&booking=B` | First-visit feedback is explicitly optional, excluded from the red personal action count, and shown separately from office work. It creates no payment or payroll hold. | tasks, UI |
| A39 | Email failure or bounce | Partial | `#/journey?panel=deliveries`; office Next actions | Outbox retry/failure tasks remain and payment emails show delivery-queue status. Sent means transport acceptance; independent inbox delivery and provider bounce ingestion are not connected. | tasks, UI |
| A40 | Background job, backup or infrastructure issue | Retained | `#/journey?panel=tasks&view=website`; `#/admin/assurance` | Automatic certificate checks and operational, job, storage and backup alerts remain in Website checks, separate from participant care tasks. | assurance, tasks |
| A41 | Alert refresh fails | Implemented | Next actions badge; `#/bookings` notice | The consolidated badge preserves its previous count on transient errors, exposes refresh failure/last refresh, and retries on focus/visibility. Revocation clears stale data; background updates do not replace draft contents. | UI |

## Source key and verification

- **UI:** `public/assets/payment-automation.js`, `public/assets/next-actions.js`, `public/assets/process-workflows.js`, `public/index.html`.
- **calendar / tasks / routes:** `public/assets/booking-calendar.js`, `lib/process-store.js`, `lib/process-routes.js`.
- **payments / Zai:** `lib/payment-automation.js`, `lib/zai-payments.js`.
- **billing / lifecycle / finance:** `lib/automatic-invoicing.js`, `lib/invoice-lifecycle.js`, `lib/process-finance.js`.
- **payroll / verification / assurance:** `lib/payroll-notifications.js`, `lib/verification-automation.js`, `lib/launch-assurance.js`.
- **server:** `server.js`, including existing booking, approval, claim and notification routes.

Focused verification performed for this review: `tests/payment-ui-tests.js` passed **24/24**, including the real backend transfer states, public versus authenticated permissions, payment preparation, stale responses and named exception actions. `tests/payroll-recovery-tests.js` passed **3/3** against an in-memory SQLite database, including old work, existing batch entries, before-cutover work, today’s work and repeated runs. These are source/interaction checks; they are not live bank or browser-layout confirmation. The release’s other verification results are recorded separately.

The remaining implementation gaps are explicit: unattended saved-payment consent/collection; connected payroll calculation, wage transfer and payslips; NDIA submission/outcome ingestion; business-bank payout/fee reconciliation; provider bounce/delivery ingestion; and the specific ageing/completeness cases marked Partial above. Provider onboarding, verified receiving-account assignments and deployed webhook/email checks are prerequisites for real external automation.
