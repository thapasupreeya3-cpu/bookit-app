THE CARE WEB v88.4.0 PAYMENT AUTOMATION UPDATE

Apply over v88.3.4, verified source commit 943ad08ec492129ff5bba3169a906812ebafdff9.
This ZIP contains changed files only. Merge its contents into the existing
repository root, replacing matching files, then redeploy. Do not replace the
whole repository with this partial ZIP.

Preserve the live database, uploaded documents/photos and protected settings.
Keep a normal pre-update backup and compare any newer/custom repository changes.
Use Node 22.23.2 (see .nvmrc), npm ci --ignore-scripts, then npm run check.
The server applies additive migrations at startup. /api/version should show
88.4.0 and schema 88400. Update a host SCHEMA_VERSION override if present.

New self-managed/private shifts invoice immediately after worker submission.
The participant reviews and can approve/pay in the same invoice page.
Plan-managed and NDIA routing retain required approval and the correct payer.
Legacy unapproved pending work is not unexpectedly invoiced by installation.
Existing approved unissued work continues through automatic recovery.

PAYMENT ACTIVATION
Read docs/PAYMENT-SETUP-v88.4.0.md. Configure server Stripe keys and its signed
webhook once; optionally enable account-supported PayTo. Ordinary bank transfer
matching requires Zai onboarding, credentials, signed webhook and verified
receiving-account assignments. An existing standard BSB/account transfer is
not automatically tracked merely by adding a Stripe payment link.

Office: Money > Payment tracking. Participant: Next actions, Bookings or
Statements > invoice. Worker: Next actions > Pay status.
Read docs/USER-GUIDE.html, docs/PAYMENT-ALERT-COVERAGE-v88.4.0.md and
validation/v88.4.0-upgrade-verification.json.

This is source code, not confirmation of deployment or provider activation.
Live payments, email, browser layout, business-bank settlement and payroll
provider transfers were not verified. Exports and office payment records are
labelled accurately; they do not mean the site sent wages.
