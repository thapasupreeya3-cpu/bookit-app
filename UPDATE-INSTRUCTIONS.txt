THE CARE WEB v88.4.13 — CURRENT UPDATE ONLY

REQUIRED BASE: the supplied v88.4.12 repository, bookit-app-main (7).zip
This ZIP contains only new or changed files for the worker task readiness fix.
It is not a replacement for the complete repository.

INSTALL ON YOUR EXISTING WEBSITE
1. Retain a backup of your existing source, database, uploads and settings.
2. Extract and MERGE this update into your v88.4.12 repository, replacing
   matching files. Keep all other files and folder contents.
3. Redeploy through your existing process using Node 22 (>=22.12 <23).
   The download must be merged into your repository before your server's
   normal update command can install it.
4. Refresh with Cmd+Shift+R (Mac) or Ctrl+Shift+R (Windows).
5. Confirm /api/version shows APP_VERSION 88.4.13 and SCHEMA_VERSION 88404.
   The schema identifier is unchanged. Queue invalidation triggers are
   installed automatically; no database reset or manual migration is needed.

WHAT TO CHECK
Admin > Today > Next actions: a blank new worker has no office action count.
Their missing uploads and profile details remain under Waiting on people.
Admin > People > Verification: select the worker. The file shows Waiting on
worker and No office action needed. Future checks are under Later office checks.
When evidence is received, the relevant office reviews become available.
The normal refresh reconciles premature tasks from the older release and
retains their history. Genuine safety restrictions and follow-ups still show.
Existing approval, eligibility and booking requirements remain enforced.

USER GUIDE: docs/USER-GUIDE.html
CHANGES: docs/CHANGELOG-v88.4.13.md
CHECKS: docs/TEST-RESULTS.md
SOURCE HASHES: RELEASE-FILES.json describes the full installed source tree.

This source update has not deployed your website. A second website is not
required. Live browser appearance remains unverified.
