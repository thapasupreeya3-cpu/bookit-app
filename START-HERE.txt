BOOKIT SITE v64 — restored with targeted animation polish
==========================================================
This folder keeps the complete v64 site: all original animations, warm photos
on the six service pages, and social-preview tags.

Targeted change only:
  - The Daily Tasks & Shared Living scene now completes the full 20-second
    kitchen story: enter together, plan and prepare, cook, carry and serve,
    eat, collect both plates one at a time, take them to the sink, then turn
    and walk naturally off-screen.
  - Existing service, homepage and background animations remain in place.

Deploy: open github.com/thapasupreeya3-cpu/bookit-app/upload/main
and drag BOTH items from inside this folder (server.js and the
public folder) into the page. Commit, then: sudo bookit-update

Safe to delete from the repo whenever you like (nothing uses them):
  index.html (the one at repo root), DESIGN.md, README-V63.md,
  get-assets.mjs, CARE_BACKGROUND_UPDATE.txt,
  public/assets/world/, public/assets/care-backgrounds/,
  public/assets/care-motion/, public/images/care-poster.webp,
  public/vendor/oo-scrub.js
